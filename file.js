/**
 * Dev Commit, B1 Featue Added
 * B1 next added
 * Dev Commit, B2 Featue Added
 * B2 nxt added
 * Hello Method
 * Development Branch
 * Working directory feature A
 * Feature A plus B
 * ! This is method to display hello count
 * ? High Priority
 * TODO: Refactor this method
 * @param {*} count
 */
function executeHello(count) {
  var loopcount = 0;
  if (count == loopcount) {
    return "Hey " + count;
  }
}

executeHello(0);
